@model FooDo2.Models.orderFood

@{
    Layout = "~/Views/Shared/orderLayout.cshtml";
    var idOrder = ViewData["idOrder"];
}

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>orderFood</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.food.name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.food.name)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.order.details)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.order.details)
        </dd>

    </dl>

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="DeleteFood" class="btn btn-default" /> |
            @Html.ActionLink("Back to List", "Index")
        </div>
    }
</div>
